services:
  db:
    image: postgres:18.4
    environment:
      POSTGRES_USER: market_user
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: market_tracker
    
    ports:
      - "5433:5432"
    
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data: